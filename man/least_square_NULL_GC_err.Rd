% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgc_optimization_error.R
\name{least_square_NULL_GC_err}
\alias{least_square_NULL_GC_err}
\title{Sum of squares minimization of the GC null model with error}
\usage{
least_square_NULL_GC_err(
  WS,
  SW,
  GC,
  cor = COR,
  snpthresh = SNPTHRESH,
  e1max = EMAX,
  e2max = EMAX,
  e1init = EINIT,
  e2init = EINIT,
  Maxit = MAXIT,
  Factr = FACTR,
  Lmm = LMM,
  Verbose = VERBOSE,
  Usegr = USEGR
)
}
\arguments{
\item{WS}{the WS observed SFS}

\item{SW}{the SW observed SFS}

\item{GC}{GC content}

\item{cor}{a Boolean to add a correction to the transformed SFS (default = TRUE)}

\item{snpthresh}{the value below which the SNP category is removed (default = 1)}

\item{e1max}{maximum for the range of WS error rate, default value = 0.49}

\item{e2max}{maximum for the range of SW error rate, default value = 0.49}

\item{e1init}{initial starting value for optimization for WS error rates, default = 0.01}

\item{e2init}{initial starting value for optimization for SW error rates, default = 0.01}

\item{Maxit}{maximum number of iterations (option for optim), see manual, default value = 100}

\item{Factr}{level of control the convergence (option for optim, see manual), default value = 10^7}

\item{Lmm}{number of updates in the method (option for optim, see manual)}

\item{Verbose}{from 0 (default) to 5: level of outputs during optimization (option for optim, see manual)}

\item{Usegr}{to use (default) or not the analytical gradient (option for optim, see manual)
It's better to use the analytical gradient function but the option can be turn off for testing}
}
\value{
A list of two lists:
\itemize{
\item param: Optimized parameters
\item criteria: Model fit criteria
}
}
\description{
Function that searches for the three parameters that minimize the sum_of_squares function
}
\examples{
sfsWS <- c(1000,500,300,200,80,50)
sfsSW <- c(2000,800,400,150,50,10)
LS <- least_square_NULL_GC_err(sfsWS,sfsSW,0.5)
LS$param$mutbias # mutation bias
LS$criteria$AIC # model AIC
}
