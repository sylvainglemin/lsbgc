% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgc_optimization_error.R
\name{least_square_hotspot2bis_err}
\alias{least_square_hotspot2bis_err}
\title{Sum of squares minimization of model Hotpsot 2bis with error}
\usage{
least_square_hotspot2bis_err(
  WS,
  SW,
  GC,
  f,
  cor = COR,
  snpthresh = SNPTHRESH,
  mincat = MINCAT,
  B0min = BMIN,
  B0max = BMAX,
  B1min = BMIN,
  B1max = BMAX,
  Mmin = MMIN,
  Mmax = MMAX,
  e1max = EMAX,
  e2max = EMAX,
  e1init = EINIT,
  e2init = EINIT,
  Maxit = MAXIT,
  Factr = FACTR,
  Lmm = LMM,
  Verbose = VERBOSE,
  Usegr = USEGR
)
}
\arguments{
\item{WS}{the WS observed SFS}

\item{SW}{the SW observed SFS}

\item{GC}{GC content}

\item{f}{proportion of hotspots (fixed by the user: 0 ≤ f ≤ 1/2)}

\item{cor}{a Boolean to add a correction to the transformed SFS (default = COR)}

\item{snpthresh}{the value below which the SNP category is removed (default = SNPTHRESH)}

\item{mincat}{minimum number of valid SNP categories to do the estimation (default = MINCAT)}

\item{B0min}{minimum for the range of B0, default value = -100}

\item{B0max}{maximum for the range of B0, default value = 100}

\item{B1min}{minimum for the range of B1, default value = -100}

\item{B1max}{maximum for the range of B1, default value = 100}

\item{Mmin}{minimum for the range of M, default value = -5}

\item{Mmax}{maximum or the range of M, default value = 5}

\item{e1max}{maximum for the range of WS error rate, default value = 0.49}

\item{e2max}{maximum for the range of SW error rate, default value = 0.49}

\item{e1init}{initial starting value for optimization for WS error rates, default = 0.01}

\item{e2init}{initial starting value for optimization for SW error rates, default = 0.01}

\item{Maxit}{maximum number of iterations (option for optim), see manual, default value = MAXIT}

\item{Factr}{level of control the convergence (option for optim, see manual), default value = FACTR}

\item{Lmm}{number of updates in the method (option for optim, see manual), default value = LMM}

\item{Verbose}{from 0 to 5: level of outputs during optimization (option for optim, see manual), default value = VERBOSE}

\item{Usegr}{to use or not the analytical gradient (option for optim, see manual), default value = USEGR}
}
\value{
A list of two lists:
\itemize{
\item param: Optimized parameters
\item criteria: Model fit criteria
}
}
\description{
Function that searches for the five parameters that minimize the sum_of_squares function
This model is the same as model 2 except that f is fixed by the user
}
\examples{
sfsWS <- c(1000,500,300,200,80,50)
sfsSW <- c(2000,800,400,150,50,10)
LS <- least_square_hotspot2bis_err(sfsWS,sfsSW,0.5,0.2)
LS$B0 # population-scaled background gBGC
LS$B1 # population-scaled hotspot gBGC
}
