% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulated_sfs_hotspot}
\alias{simulated_sfs_hotspot}
\title{Simulated SFS hotspot model}
\usage{
simulated_sfs_hotspot(
  n,
  theta,
  B0,
  B1,
  f,
  mut_bias,
  GC,
  eWS = 0,
  eSW = 0,
  fix_snp = "free"
)
}
\arguments{
\item{n}{sample size}

\item{theta}{population scale mutation rate for WS mutations = 4NeuWS}

\item{B0}{population scale gBGC coefficient for the background = 4Neb0}

\item{B1}{population scale gBGC coefficient for hotspots = 4Neb1}

\item{f}{fraction of hotspots}

\item{mut_bias}{S to W mutation bias so uSW = mut_bias*uWS}

\item{GC}{GC content}

\item{eWS}{polarization error for WS mutations}

\item{eSW}{polarization error for SW mutations}

\item{fix_snp}{choice between three possibilities:
"free" (default): in each frequency class a Poisson number of SNPs is drawn so that the total number is also a random variable.
"fixed_sfs": for each SFS (WS and SW) the number of sampled SNPs is fixed to the expected number of the model.
Then a multinomial sampling is drawn for each SFS
"fixed_tot": the total number of sampled SNPs is fixed (WS + SW)
Then a multinomial sampling is drawn fro the combined SFS}
}
\value{
the expected SFS
}
\description{
Generate a random WS and SW SFS.
If B0 = B1 = 0 it corresponds to the neutral model.
Yet, if you want to generate a neutral sfs it is simpler to use the "simulated_sfs_constant" function
}
\examples{
sim_sfs <- simulated_sfs_constant(20,100,1,2,0.5,0,0)
barplot(sim_sfs$WS)
barplot(sim_sfs$SW)
}
