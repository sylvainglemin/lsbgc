% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_data.R
\name{t_variance}
\alias{t_variance}
\title{Expected variance of the transformed sfs
Assuming that each class follows a Poisson dsitribution}
\usage{
t_variance(sfs, cor = COR)
}
\arguments{
\item{sfs}{a site frequency spectrum (a numerical vector)}

\item{cor}{a Boolean to add a correction to the transformed SFS (default = FALSE)}
}
\value{
the expected variance of the sfs
}
\description{
Expected variance of the transformed sfs
Assuming that each class follows a Poisson dsitribution
}
\examples{
#' A WS and SW SFS
sfs_ws <- c(100,50,30,20,3)
sfs_sw <- c(300,140,80,50,7)
# Transformed ratio with bias correction
t_ratio <- t_sfs(sfs_ws) - t_sfs(sfs_sw)
# The weight for each class is given by the inverse of the variance
weight <- 1/(t_variance(sfs_ws) + t_variance(sfs_sw))
}
